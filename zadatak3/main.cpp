#include <iostream>
#include "bin_stablo_ch.h"

/*using namespace std;

void visit(char & ch)
{
    std::cout << ch << " ";
}

int main()
{

    BinarySTree<char> bst;

    std::cout << bst.empty() << std::endl;
    char cset[]={'t','p','q','s','g','v','y','l','h'};

    for(auto c : cset){ bst.insert(c); //bst.inOrder(visit); cout << endl;
                                }

    std::cout << "No of elements = " << bst.size() << "\n";

    std::cout << "Preorder :\n";
    bst.preOrder(visit);
    std::cout << "\n";

    std::cout << "Postorder :\n";
    bst.postOrder(visit);
    std::cout << "\n";

    std::cout << "Inorder :\n";
    bst.inOrder(visit);
    std::cout << "\n";

    std::cout << bst.isInTree('t') <<" " << bst.isInTree('d') << "\n";

    return 0;
}*/
